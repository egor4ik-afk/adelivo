# prisma/schema.prisma — город магазина

# В модель Shop, рядом со storeAddress:

  /// Город магазина: центр карты и приоритет при распознавании адресов.
  /// Хранится кодом («msk», «spb»), список — в src/lib/cities.ts.
  city          String? @default("msk")

# После правки:
#   npx prisma db push && npx prisma generate
# Поле с дефолтом, данные не трогает: у существующих магазинов
# останется Москва, как и было зашито в коде.
